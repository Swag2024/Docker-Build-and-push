name: tictoc-app
on: 
  push:
    branches: main

  pull_request: 
    branches: main

jobs:
  test:
    name: unit Testing
    runs-on: ubuntu-latest
    steps:
    -  name: checkout
       uses: actions/checkout@v5
      
    

